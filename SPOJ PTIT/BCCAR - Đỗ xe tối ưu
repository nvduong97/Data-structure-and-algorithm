https://www.spoj.com/PTIT/problems/BCCAR/

// do xe toi uu
#include<bits/stdc++.h>
using namespace std;
main(){
	int t,n,i,j,a[21];
	cin >> t;
	while(t--){
		cin >> n;
		for(i=0; i<n; i++){
			cin >> a[i];
		}
		sort(a,a+n);
		cout << 2*(a[n-1]-a[0]) << endl;
	}
	return 0;
}
